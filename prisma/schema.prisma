// Add to existing schema
model Resource {
  id          String   @id @default(cuid())
  name        String
  type        String
  url         String
  contentType String?  // Add this field
  dealRoomId  String
  dealRoom    DealRoom @relation(fields: [dealRoomId], references: [id])
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}